// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/a10adotapp/a10a.app/ent/linenft"
	"github.com/a10adotapp/a10a.app/ent/linenftactivity"
	"github.com/a10adotapp/a10a.app/ent/linenftmillionarthursproperty"
	"github.com/a10adotapp/a10a.app/ent/predicate"
)

// LINENFTQuery is the builder for querying LINENFT entities.
type LINENFTQuery struct {
	config
	ctx                        *QueryContext
	order                      []linenft.OrderOption
	inters                     []Interceptor
	predicates                 []predicate.LINENFT
	withActivities             *LINENFTActivityQuery
	withMillionArthursProperty *LINENFTMillionArthursPropertyQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LINENFTQuery builder.
func (lq *LINENFTQuery) Where(ps ...predicate.LINENFT) *LINENFTQuery {
	lq.predicates = append(lq.predicates, ps...)
	return lq
}

// Limit the number of records to be returned by this query.
func (lq *LINENFTQuery) Limit(limit int) *LINENFTQuery {
	lq.ctx.Limit = &limit
	return lq
}

// Offset to start from.
func (lq *LINENFTQuery) Offset(offset int) *LINENFTQuery {
	lq.ctx.Offset = &offset
	return lq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lq *LINENFTQuery) Unique(unique bool) *LINENFTQuery {
	lq.ctx.Unique = &unique
	return lq
}

// Order specifies how the records should be ordered.
func (lq *LINENFTQuery) Order(o ...linenft.OrderOption) *LINENFTQuery {
	lq.order = append(lq.order, o...)
	return lq
}

// QueryActivities chains the current query on the "activities" edge.
func (lq *LINENFTQuery) QueryActivities() *LINENFTActivityQuery {
	query := (&LINENFTActivityClient{config: lq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(linenft.Table, linenft.FieldID, selector),
			sqlgraph.To(linenftactivity.Table, linenftactivity.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, linenft.ActivitiesTable, linenft.ActivitiesColumn),
		)
		fromU = sqlgraph.SetNeighbors(lq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMillionArthursProperty chains the current query on the "million_arthurs_property" edge.
func (lq *LINENFTQuery) QueryMillionArthursProperty() *LINENFTMillionArthursPropertyQuery {
	query := (&LINENFTMillionArthursPropertyClient{config: lq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(linenft.Table, linenft.FieldID, selector),
			sqlgraph.To(linenftmillionarthursproperty.Table, linenftmillionarthursproperty.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, linenft.MillionArthursPropertyTable, linenft.MillionArthursPropertyColumn),
		)
		fromU = sqlgraph.SetNeighbors(lq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LINENFT entity from the query.
// Returns a *NotFoundError when no LINENFT was found.
func (lq *LINENFTQuery) First(ctx context.Context) (*LINENFT, error) {
	nodes, err := lq.Limit(1).All(setContextOp(ctx, lq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{linenft.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lq *LINENFTQuery) FirstX(ctx context.Context) *LINENFT {
	node, err := lq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LINENFT ID from the query.
// Returns a *NotFoundError when no LINENFT ID was found.
func (lq *LINENFTQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = lq.Limit(1).IDs(setContextOp(ctx, lq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{linenft.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lq *LINENFTQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := lq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LINENFT entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LINENFT entity is found.
// Returns a *NotFoundError when no LINENFT entities are found.
func (lq *LINENFTQuery) Only(ctx context.Context) (*LINENFT, error) {
	nodes, err := lq.Limit(2).All(setContextOp(ctx, lq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{linenft.Label}
	default:
		return nil, &NotSingularError{linenft.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lq *LINENFTQuery) OnlyX(ctx context.Context) *LINENFT {
	node, err := lq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LINENFT ID in the query.
// Returns a *NotSingularError when more than one LINENFT ID is found.
// Returns a *NotFoundError when no entities are found.
func (lq *LINENFTQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = lq.Limit(2).IDs(setContextOp(ctx, lq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{linenft.Label}
	default:
		err = &NotSingularError{linenft.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lq *LINENFTQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := lq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LINENFTs.
func (lq *LINENFTQuery) All(ctx context.Context) ([]*LINENFT, error) {
	ctx = setContextOp(ctx, lq.ctx, "All")
	if err := lq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LINENFT, *LINENFTQuery]()
	return withInterceptors[[]*LINENFT](ctx, lq, qr, lq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lq *LINENFTQuery) AllX(ctx context.Context) []*LINENFT {
	nodes, err := lq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LINENFT IDs.
func (lq *LINENFTQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if lq.ctx.Unique == nil && lq.path != nil {
		lq.Unique(true)
	}
	ctx = setContextOp(ctx, lq.ctx, "IDs")
	if err = lq.Select(linenft.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lq *LINENFTQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := lq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lq *LINENFTQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lq.ctx, "Count")
	if err := lq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lq, querierCount[*LINENFTQuery](), lq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lq *LINENFTQuery) CountX(ctx context.Context) int {
	count, err := lq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lq *LINENFTQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lq.ctx, "Exist")
	switch _, err := lq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lq *LINENFTQuery) ExistX(ctx context.Context) bool {
	exist, err := lq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LINENFTQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lq *LINENFTQuery) Clone() *LINENFTQuery {
	if lq == nil {
		return nil
	}
	return &LINENFTQuery{
		config:                     lq.config,
		ctx:                        lq.ctx.Clone(),
		order:                      append([]linenft.OrderOption{}, lq.order...),
		inters:                     append([]Interceptor{}, lq.inters...),
		predicates:                 append([]predicate.LINENFT{}, lq.predicates...),
		withActivities:             lq.withActivities.Clone(),
		withMillionArthursProperty: lq.withMillionArthursProperty.Clone(),
		// clone intermediate query.
		sql:  lq.sql.Clone(),
		path: lq.path,
	}
}

// WithActivities tells the query-builder to eager-load the nodes that are connected to
// the "activities" edge. The optional arguments are used to configure the query builder of the edge.
func (lq *LINENFTQuery) WithActivities(opts ...func(*LINENFTActivityQuery)) *LINENFTQuery {
	query := (&LINENFTActivityClient{config: lq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lq.withActivities = query
	return lq
}

// WithMillionArthursProperty tells the query-builder to eager-load the nodes that are connected to
// the "million_arthurs_property" edge. The optional arguments are used to configure the query builder of the edge.
func (lq *LINENFTQuery) WithMillionArthursProperty(opts ...func(*LINENFTMillionArthursPropertyQuery)) *LINENFTQuery {
	query := (&LINENFTMillionArthursPropertyClient{config: lq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lq.withMillionArthursProperty = query
	return lq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LINENFT.Query().
//		GroupBy(linenft.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lq *LINENFTQuery) GroupBy(field string, fields ...string) *LINENFTGroupBy {
	lq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LINENFTGroupBy{build: lq}
	grbuild.flds = &lq.ctx.Fields
	grbuild.label = linenft.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at"`
//	}
//
//	client.LINENFT.Query().
//		Select(linenft.FieldCreatedAt).
//		Scan(ctx, &v)
func (lq *LINENFTQuery) Select(fields ...string) *LINENFTSelect {
	lq.ctx.Fields = append(lq.ctx.Fields, fields...)
	sbuild := &LINENFTSelect{LINENFTQuery: lq}
	sbuild.label = linenft.Label
	sbuild.flds, sbuild.scan = &lq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LINENFTSelect configured with the given aggregations.
func (lq *LINENFTQuery) Aggregate(fns ...AggregateFunc) *LINENFTSelect {
	return lq.Select().Aggregate(fns...)
}

func (lq *LINENFTQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lq); err != nil {
				return err
			}
		}
	}
	for _, f := range lq.ctx.Fields {
		if !linenft.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lq.path != nil {
		prev, err := lq.path(ctx)
		if err != nil {
			return err
		}
		lq.sql = prev
	}
	return nil
}

func (lq *LINENFTQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LINENFT, error) {
	var (
		nodes       = []*LINENFT{}
		_spec       = lq.querySpec()
		loadedTypes = [2]bool{
			lq.withActivities != nil,
			lq.withMillionArthursProperty != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LINENFT).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LINENFT{config: lq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := lq.withActivities; query != nil {
		if err := lq.loadActivities(ctx, query, nodes,
			func(n *LINENFT) { n.Edges.Activities = []*LINENFTActivity{} },
			func(n *LINENFT, e *LINENFTActivity) { n.Edges.Activities = append(n.Edges.Activities, e) }); err != nil {
			return nil, err
		}
	}
	if query := lq.withMillionArthursProperty; query != nil {
		if err := lq.loadMillionArthursProperty(ctx, query, nodes, nil,
			func(n *LINENFT, e *LINENFTMillionArthursProperty) { n.Edges.MillionArthursProperty = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (lq *LINENFTQuery) loadActivities(ctx context.Context, query *LINENFTActivityQuery, nodes []*LINENFT, init func(*LINENFT), assign func(*LINENFT, *LINENFTActivity)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint32]*LINENFT)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.LINENFTActivity(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(linenft.ActivitiesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.linenft_activities
		if fk == nil {
			return fmt.Errorf(`foreign-key "linenft_activities" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "linenft_activities" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (lq *LINENFTQuery) loadMillionArthursProperty(ctx context.Context, query *LINENFTMillionArthursPropertyQuery, nodes []*LINENFT, init func(*LINENFT), assign func(*LINENFT, *LINENFTMillionArthursProperty)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint32]*LINENFT)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.LINENFTMillionArthursProperty(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(linenft.MillionArthursPropertyColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.linenft_million_arthurs_property
		if fk == nil {
			return fmt.Errorf(`foreign-key "linenft_million_arthurs_property" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "linenft_million_arthurs_property" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (lq *LINENFTQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lq.querySpec()
	_spec.Node.Columns = lq.ctx.Fields
	if len(lq.ctx.Fields) > 0 {
		_spec.Unique = lq.ctx.Unique != nil && *lq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lq.driver, _spec)
}

func (lq *LINENFTQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(linenft.Table, linenft.Columns, sqlgraph.NewFieldSpec(linenft.FieldID, field.TypeUint32))
	_spec.From = lq.sql
	if unique := lq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lq.path != nil {
		_spec.Unique = true
	}
	if fields := lq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, linenft.FieldID)
		for i := range fields {
			if fields[i] != linenft.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lq *LINENFTQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lq.driver.Dialect())
	t1 := builder.Table(linenft.Table)
	columns := lq.ctx.Fields
	if len(columns) == 0 {
		columns = linenft.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lq.sql != nil {
		selector = lq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lq.ctx.Unique != nil && *lq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lq.predicates {
		p(selector)
	}
	for _, p := range lq.order {
		p(selector)
	}
	if offset := lq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LINENFTGroupBy is the group-by builder for LINENFT entities.
type LINENFTGroupBy struct {
	selector
	build *LINENFTQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lgb *LINENFTGroupBy) Aggregate(fns ...AggregateFunc) *LINENFTGroupBy {
	lgb.fns = append(lgb.fns, fns...)
	return lgb
}

// Scan applies the selector query and scans the result into the given value.
func (lgb *LINENFTGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lgb.build.ctx, "GroupBy")
	if err := lgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LINENFTQuery, *LINENFTGroupBy](ctx, lgb.build, lgb, lgb.build.inters, v)
}

func (lgb *LINENFTGroupBy) sqlScan(ctx context.Context, root *LINENFTQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lgb.fns))
	for _, fn := range lgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lgb.flds)+len(lgb.fns))
		for _, f := range *lgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LINENFTSelect is the builder for selecting fields of LINENFT entities.
type LINENFTSelect struct {
	*LINENFTQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ls *LINENFTSelect) Aggregate(fns ...AggregateFunc) *LINENFTSelect {
	ls.fns = append(ls.fns, fns...)
	return ls
}

// Scan applies the selector query and scans the result into the given value.
func (ls *LINENFTSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ls.ctx, "Select")
	if err := ls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LINENFTQuery, *LINENFTSelect](ctx, ls.LINENFTQuery, ls, ls.inters, v)
}

func (ls *LINENFTSelect) sqlScan(ctx context.Context, root *LINENFTQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ls.fns))
	for _, fn := range ls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
